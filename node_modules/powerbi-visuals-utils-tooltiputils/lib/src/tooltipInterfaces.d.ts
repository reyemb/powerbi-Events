import powerbi from "powerbi-visuals-api";
import { Selection } from 'd3-selection';
import ITooltipService = powerbi.extensibility.ITooltipService;
export interface TooltipEventArgs<TData> {
    data: TData;
    coordinates: number[];
    elementCoordinates: number[];
    context: HTMLElement;
    isTouchEvent: boolean;
}
export interface ITooltipServiceWrapper {
    addTooltip<T>(selection: Selection<any, any, any, any>, getTooltipInfoDelegate: (datapoint: T) => powerbi.extensibility.VisualTooltipDataItem[], getDataPointIdentity?: (datapoint: T) => powerbi.extensibility.ISelectionId, reloadTooltipDataOnMouseMove?: boolean): void;
    hide(): void;
    cancelTouchTimeoutEvents(): void;
}
export interface TooltipEnabledDataPoint {
    tooltipInfo?: powerbi.extensibility.VisualTooltipDataItem[];
}
export interface TooltipServiceWrapperOptions {
    tooltipService: ITooltipService;
    rootElement?: Element;
    handleTouchDelay?: number;
}
